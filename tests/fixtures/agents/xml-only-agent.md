This is an agent file without frontmatter.

It only has XML content.

```xml
<agent id="xml-only.md" name="XmlBot" title="XML Only Agent" icon="📄">
  <persona>
    <role>XML Role</role>
  </persona>
</agent>
```
